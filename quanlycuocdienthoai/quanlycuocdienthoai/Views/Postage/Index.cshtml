@model IEnumerable<quanlycuocdienthoai.Models.Postage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateApplied)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateApplied)
        </td>
        @*
        <td>
            @Html.ActionLink("Chi tiết", "Details", new { id=item.KeyId })
        </td>
        *@
    </tr>
}

</table>
